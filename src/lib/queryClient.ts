/** @file Shared TanStack Query client configuration. */
import { QueryClient } from "@tanstack/react-query";

/** Shared TanStack Query client for server-state caching. */
export const queryClient = new QueryClient();
